{
  "name": "krisp-webhook",
  "description": "Krisp webhook receiver → Google Drive",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "googleapis": "^148.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "private": true
}
